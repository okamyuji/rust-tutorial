name: CI
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  # 章ごとに独立した crate のため、直下の Cargo.toml を前提とする中央の rust-ci.yml は使わず、章単位で検査する
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        chapter: [chapter01, chapter02, chapter03, chapter04, chapter05]
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: code/${{ matrix.chapter }}
      - name: Format
        run: cargo fmt --all --check --manifest-path code/${{ matrix.chapter }}/Cargo.toml
      - name: Clippy
        run: cargo clippy --all-targets --all-features --locked --manifest-path code/${{ matrix.chapter }}/Cargo.toml -- -D warnings
      - name: Test
        run: cargo test --all-targets --locked --manifest-path code/${{ matrix.chapter }}/Cargo.toml
  security:
    permissions:
      contents: read
      pull-requests: write
    uses: okamyuji/reusable-workflows/.github/workflows/security-scan.yml@v1
